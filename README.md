## Optional AI Provider

The monitor can add an AI summary to the email report through Qwen, Kimi, or any OpenAI-compatible API.

See `AI_PROVIDER_SETUP.md` for environment variables and GitHub Secrets setup.
